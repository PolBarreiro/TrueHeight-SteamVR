// ============================================================================
//  TrueHeight - resource identifiers
//
//  Only IDI_APPICON belongs here.
//
//  Every control and menu ID (IDC_*, IDM_TRAY_*) is already declared in the
//  enum near the top of src/main.cpp. Do NOT redeclare them in this file:
//  the preprocessor would substitute the enum names for numbers and the
//  build would fail with "expected identifier before numeric constant".
// ============================================================================
#pragma once

#define IDI_APPICON 101
